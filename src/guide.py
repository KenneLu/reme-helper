"""内置的控制台说明。

以前这份说明是从 ReMe 安装目录里读一个 Markdown 文件，问题是：
  * 那份文件是某一台机器上写出来的（路径、SSH 别名、验证日期都是私人的），不具备通用性；
  * 普通用户装完 ReMe 并不会带着这份文件，于是「打开控制台说明」直接报路径不存在。

所以改成随工具分发的内置内容：
  * 正文用占位符讲通用做法，不写任何机器专属路径；
  * 「本机路径」那一节在打开时**现场生成**（真实值来自当前配置，不是写死的）；
  * 每行都走 i18n 词表，英文模式下整篇自动变英文（命令/路径那些没有中文的行原样保留）。
"""
from __future__ import annotations

# @@PATHS@@ 会被替换成现场生成的「本机路径」表
GUIDE_LINES: list[str] = [
    "# ReMe 助手 · 控制台说明",
    "",
    "ReMe 助手是一个 Windows 托盘小工具：帮你选 ReMe 的运行模式、看清每项配置的实际影响、启停服务，",
    "以及把 Windows 上的 ReMe 反向映射给虚拟机里的客户端。它不替代 ReMe，也不修改 ReMe 安装包里的官方配置。",
    "",
    "## 本机路径",
    "",
    "@@PATHS@@",
    "这些路径随时可以在托盘菜单的「打开指引（文件在哪）…」里查看、打开或复制。",
    "",
    "## 三种模式",
    "",
    "| 模式 | 配置文件 | 能力与前提 |",
    "|---|---|---|",
    "| 基础模式 | 基础配置 | 不调用模型：Markdown 读写、BM25 与关系检索、Studio、MCP、索引维护 |",
    "| 全功能模式 | 完整配置 | 官方默认全开：自动记忆与整理、资料卡片、内部 Chat；需要填好 LLM |",
    "| 自定义模式 | 自定义配置 | 逐项挑选，从官方默认生成；能满足依赖就能开 |",
    "",
    "前两项是固定预设：改动其中任意一项都会自动转为自定义模式，并且只应用你改的那一项（可撤销）。",
    "模式是由配置内容推导出来的，不是单独存的一个开关——草稿与某个预设完全一致时会自动切回该预设。",
    "「保存」会写配置、按需重启服务，并保持在窗口里，方便继续调整。",
    "",
    "## 启动与停止",
    "",
    "- 托盘菜单或控制台按钮都可以启停；控制台会显示服务状态与健康检查结果。",
    "- 服务地址默认是 http://127.0.0.1:2333/ ，健康检查在 /health_check ，Studio 就在同一个地址。",
    "- 工具只关闭「自己启动或接管」的服务；外部启动的 ReMe 不会被偷偷杀掉。",
    "- 端口被占用时先看日志目录里的最新日志，再换端口或用 service.port=<端口> 启动。",
    "",
    "## LLM 与 Embedding",
    "",
    "- 需要 LLM 的能力：自动记忆、自动整理、内部 Chat。没有填 LLM 时这些能力会显示为未启用。",
    "- 需要 Embedding 的能力：语义检索（近义、改写、中英混搜）与近似检索加速。只用关键词检索时可以不配。",
    "- 地址、模型、Key 都可以在控制台里填；保存时会自动写入 .env，不必手工编辑。",
    "- Key 字段按密码框处理：已经配置的会载入并显示为掩码，点「显示」才看得到明文，留空表示不改。",
    "- 「测试连接」验证两件事：接口可用，以及能按要求输出结构化 JSON（自动整理依赖它）。",
    "- 「测试 Embedding」验证接口可用、维度一致，以及近义句确实比无关句更接近。",
    "",
    "## 记忆是怎么长出来的",
    "",
    "- Auto Memory：把对话提炼成当天的记忆卡片（daily）。",
    "- Auto Dream：每天把有变化的 daily 沉淀成长期节点（digest），并维护兴趣主题。",
    "- 扫描天数决定这次整理回看几天：内容没变的日记本来就不会被抽取，整窗无变化时整次整理直接跳过；",
    "  窗口越大，只要其中任意一天有变化，这次抽取读入的内容就越多——更慢更贵，不会更准。",
    "- 每次最多沉淀决定一次能写几个长期节点：每个节点都要单独调用一次模型，调大只会更慢更贵。",
    "- 「立即整理一次」适合手动补一次；它需要服务在运行、LLM 可用，并会消耗 token。",
    "- 整理结果会记在日志里，控制台下方会显示上次整理的时间与产出。",
    "",
    "## 让别的客户端用上 ReMe",
    "",
    "ReMe 通过 MCP 暴露记忆工具，控制台的「MCP 工具暴露」里可以逐项开关。Codex 之类的客户端在配置里加：",
    "",
    "```toml",
    "[mcp_servers.reme]",
    "url = \"http://127.0.0.1:2333/mcp\"",
    "```",
    "",
    "只读工具（检索、读文件、关系遍历）随时可用；写入类工具（新建、修改记忆）打开后，客户端才能精确写入。",
    "多个客户端共用同一份记忆：整理交给 ReMe 自己的计划任务，客户端只负责读写，不要在客户端重复开启自动整理。",
    "",
    "## 给虚拟机里的客户端用",
    "",
    "ReMe 只监听本机回环地址，所以用 SSH 反向转发把它映射给虚拟机。在 Windows 上执行（把占位符换成你的目标）：",
    "",
    "```powershell",
    "ssh -N -R 127.0.0.1:<VM_PORT>:127.0.0.1:2333 <user@host>",
    "```",
    "",
    "虚拟机里再把客户端指向 http://127.0.0.1:<VM端口>/mcp 即可。托盘菜单的「VM 目标」可以添加多个目标，",
    "分别配置主机、SSH 端口、私钥与映射端口，并随 ReMe 一起启停。工具只负责建立和监测隧道，",
    "不生成密钥、不改 SSH 配置、不动防火墙。",
    "",
    "映射端口不要复用机器上其它工具已占用的本地端口，挑一个空闲的高位端口即可，否则隧道建不起来。",
    "",
    "## 接入客户端",
    "",
    "- Codex 之类的客户端在配置里加 `url = \"http://127.0.0.1:2333/mcp\"` 就能用它的记忆工具。",
    "- DeepSeek Harness 另有官方 ReMe 插件（见官方文档的 typescript 一节）。装之前先卸掉别的记忆插件，",
    "  不要同时启用两套自动记忆，否则同一段对话会被整理两遍。",
    "",
    "## 出问题时",
    "",
    "- 服务起不来：先看日志目录里最新的日志，再用 /health_check 确认端口是否有响应。",
    "- 记忆不更新：确认自动记忆与整理已开启、LLM 可用、服务在运行，且当天确实有对话。",
    "- 检索不到：先看是关键词检索还是语义检索；语义检索需要 Embedding 配置正确、维度与索引一致，",
    "  改过维度或模型后要重建一次索引。",
    "- 界面看不清或想换语言：控制台右上角切换深色/浅色与中英，选择会被记住。",
    "",
    "## 备份与升级",
    "",
    "需要长期备份的是记忆数据（workspace）、配置和 .env 三样；程序环境可以按官方方式重建。",
    "升级前先复制一份记忆数据，用副本验证新版本，再切换正式启动方式。",
    "",
    "三种模式的配置（app.yaml / app-full.yaml / app-custom.yaml）都由本工具从 ReMe 自带的",
    "default.yaml 现场生成，装完就有、随时可重建，不需要手工维护。升级 ReMe 之后回到这里",
    "重新保存一次，三份配置就会按新版本的 default.yaml 重建。",
    "",
    "## 官方资料",
    "",
    "- <https://github.com/agentscope-ai/ReMe>",
    "- <https://github.com/agentscope-ai/ReMe/blob/main/docs/zh/quick_start.md>",
    "- <https://github.com/agentscope-ai/ReMe/blob/main/docs/zh/configuration.md>",
]


def guide_markdown(context: dict) -> str:
    """把内置说明渲染成 Markdown；``context`` 里的本机路径现场生成。"""
    from modules.i18n.i18n import translate  # 局部导入：guide 只被 main 用，避免循环依赖

    lang = context.get("lang") or "zh"
    rows = [
        translate("| 项目 | 本机取值 |", lang),
        "|---|---|",
        f"| {translate('安装目录', lang)} | `{context.get('reme_root', '')}` |",
        f"| {translate('当前模式', lang)} | {context.get('mode_label', '')} |",
        f"| {translate('当前配置文件', lang)} | `{context.get('config_path', '')}` |",
        f"| {translate('记忆数据', lang)} | `{context.get('workspace', '')}` |",
        f"| {translate('凭据文件', lang)} | `{context.get('env_path', '')}` |",
        f"| {translate('日志目录', lang)} | `{context.get('logs', '')}` |",
        f"| {translate('服务地址', lang)} | {context.get('service_url', '')} |",
        f"| {translate('工具版本', lang)} | {context.get('version', '')} |",
    ]
    out: list[str] = []
    for line in GUIDE_LINES:
        if line == "@@PATHS@@":
            out.extend(rows)
            continue
        out.append(translate(line, lang) if line else line)
    return "\n".join(out) + "\n"
