# 更新记录

本工具的开发记录（中文）。面向使用者的入口文档见 [中文 README](README.zh-CN.md) / [English README](README.md)；
把客户端接入 ReMe 的步骤见 `doc/zh/setup.md`（也可在应用里「阅读接入文档」）。

## v1.0.16

- **检查更新撞上限流时，说清楚是怎么回事**。用户实际遇到的提示是：

  ```
  检查更新失败：HTTP Error 403: rate limit exceeded
  ```

  准确，但毫无用处。匿名访问 `api.github.com` 是**每个出口 IP 每小时 60 次**，而且与同一网络下的其他工具**共用**这份额度 —— 很容易在无意中用光（本机就是：调试期间轮询发布状态把额度烧完，随后用户的检查更新跟着失败，与他的配置毫无关系）。
  现在会补一句：这是 GitHub 的匿名额度、是共享的、过几分钟再试即可、**不是配置问题**（识别 403 / 429）。

## v1.0.15

- **每次更新都在 `%TEMP%` 里漏掉一整个包（实测 201 MB）**。`download_and_apply_helper_update()` 用 `tempfile.mkdtemp` 建暂存目录，把下载的 zip 与解压结果都放在那儿；更新器把文件铺进安装目录后**只删掉自己**，从不删暂存目录，而 Python 侧是 `os._exit` 退出、也不会清。于是每次更新永久留下约 **50 MB**（那正是解压后的整包）。实测四次更新积了 4 个目录、共 **201.3 MB**。
  修在两个地方：① 更新器模板在 `:cleanup` 里 `rmdir /s /q "%WORK%"` —— 放在收尾段而不是成功分支上，是为了让"等不到旧进程退出而放弃"那条路径也一并清掉；② 启动时做一次兜底清扫，只动本应用自己命名的 `reme-helper-update-*`，且**只动一小时前的**（正在进行的更新，其暂存目录是刚建的，绝不能碰）。
- **托盘图标没能注册时，明确告诉用户**。这是「应用活着、用户却完全碰不到它」的那一类故障：进程正常、窗口正常、日志安静，唯独通知区里没有它的图标。实测代价是用户反复报告"点不动"、排查了三轮才靠新加的探针找到根因 —— 而在此之前，**唯一的线索是用户自己的描述**。现在注册失败会弹对话框说明原因与处置办法，并把 `GetLastError()` 一并记进日志（以前只记返回值为 0，说不出为什么）。
- **`notify()` 不再静默吞掉异常**。气泡是所有托盘动作的反馈通道，它坏掉的表现与"点了没反应"**一模一样** —— 正是本次最难分辨的那一类。以前是 `except Exception: pass`，现在至少留一行日志。
- **顺手清掉两个陈旧产物**：`src/settings-ui-test.log`（2026-09-11 的遗留，当前代码写在 `log/tests/`，它只是被 `.gitignore` 藏住了）与 `%TEMP%` 里已经泄漏的那 4 个暂存目录。

## v1.0.14

- **托盘加了诊断日志（这一次是"日志查不出来就加日志"）**。托盘「点了没反应」有两种**从外面看一模一样**的原因，之前的日志对二者都沉默：

  1. **外壳根本没把点击投递进来** —— 最典型的是点在了**残留的幽灵图标**上（那个进程早没了，图标却还留在通知区，Windows 10 的图标缓存 `IconStreams` 会留住它）；
  2. **点击到了，但菜单没弹出来** —— pystray 弹菜单前必须先 `SetForegroundWindow`（win32 后端 `_on_notify`），Windows 的前台锁定会让它失败，失败时 `TrackPopupMenuEx` 立刻返回 0。

  实测过：对一个存活的实例注入 `WM_NOTIFY`，菜单**能**弹出来 —— 也就是说应用侧是好的，问题在外壳到进程这一段，而这一段此前完全没有观测点。

  现在记录三件事：
  - **每一次托盘通知**（`lparam` 与它是什么：左键/右键/其他），以及处理完之后**前台窗口是不是自己**（不是的话菜单必然弹不出来）。一次用户交互一行。
  - **`Shell_NotifyIcon` 的返回值**。这一点尤其重要：**pystray 把这个返回值丢掉了**（`_message` 只调用、不看结果），所以 `NIM_ADD` 失败时没有任何异常 —— 窗口照建、注入消息照样能弹菜单，但外壳里根本没有这个图标，真实点击无处可去，日志一片安静。失败时还会把**调用者**一起写下来。
  - **启动时间线**（`icon created` / `icon shown` 各带耗时），1.0.12 修的那个"图标迟到 20 秒"如果回来，第一眼就能看见。
- **`sync_autostart_path()` 只在打包态生效**：从源码运行时 `sys.executable` 是 `python.exe`，照做会把开机自启改成 `python.exe`——实测踩到过一次。开发态没有"安装位置"这个概念，本就不该动注册表。
- **`monitor_loop` 不再对尚未注册的图标发 `NIM_MODIFY`**：1.0.12 把它的首轮改成立即执行之后，它会抢在 `visible = True`（`NIM_ADD`）之前去换图标，而那时外壳里还没有这条记录，必然失败。（首次显示前 pystray 自己也会发一次注定失败的 `NIM_MODIFY` —— 那是它的前奏，日志里已按预期处理，不再报成故障。）

## v1.0.13

- **两个版本行串台（真 bug，不只是措辞）**：`UPDATE_STATE` 被定义了**两次** —— ReMe 用的那份 `{checked_for, latest, at, error}` 与助手用的那份 `{checked, latest, newer, detail}`，**后者在模块加载时把前者覆盖掉**，于是两个检查共用同一个字典。两者都有 `latest` 键，所以「先查 ReMe、再查助手」之后，助手的新版本号会显示在 **ReMe 版本行**上——用户看到的就是「ReMe版本：0.4.1.11（有新版 v1.0.12）」。连带还有两处：`reme_upgrade_prompt` 会把助手的 tag 当成 ReMe 的升级目标写进给 AI 的提示词，反之亦然。现在拆成 `REME_UPDATE_STATE` 与 `HELPER_UPDATE_STATE`，各报各的结论。
- **托盘菜单重新分区**：`ReMe版本` 与 `ReMe助手版本` 分成两行；下面按同一顺序分成两组 —— ReMe 组（`检查 ReMe 更新…` · `复制 ReMe 更新步骤（交给 AI 执行）`）、ReMe 助手组（`检查 ReMe 助手更新…` · `下载并更新 ReMe 助手` · `复制 ReMe 助手更新步骤（交给 AI 执行）`）。原先两组的顺序是交错的，两个「复制…」也没点明是谁的。
- **检查更新与执行更新改走对话框**。用户主动点了这一项，他会**等着看结果**，而气泡会被系统的专注助手吞掉；「已经是最新」本来也没有别的反馈，用户只会以为点了没反应。更新成功尤其如此——进程马上要退出，气泡根本来不及被看见（实测用户就是因为这个以为更新失败）。
  现在四种情况都有对话框：已是最新 → 一个「好」；有新版本 → 「立即更新 / 稍后」；检查失败 → 「复制…（交给 AI 执行） / 关闭」；更新失败 → 同样是「复制…（交给 AI 执行） / 关闭」；更新已交出 → 「立即重启」。ReMe 与 ReMe 助手两条链路都如此。右键菜单项照旧保留。
- **对话框差点做成"看不见的窗口"**：`ui_dialog` 一开始照抄了既有写法 `dialog.transient(parent)`，而本应用的常驻根窗口**一直是隐藏的**（它只当 Tk 解释器用），控制台没开着时 `ui_parent()` 返回的正是它。实测 Tk 的行为是：**master 处于 withdrawn 时，设成它 transient 的 Toplevel 会跟着被 withdraw，`deiconify()` 也救不回来**：

  | 变体（master 为隐藏根窗口） | 结果 |
  |---|---|
  | `transient(parent)` | `ismapped=0 viewable=0 state=withdrawn` |
  | 不设 transient | `ismapped=1 viewable=1 state=normal` |
  | `transient` + `deiconify()` | 仍然 `withdrawn` |

  照抄的话，用户点「检查更新」会**什么都不出现**。现在加了一个 `attach_dialog()`：只在父窗口**真的可见**时才 `transient`；`choose_from_list` 有同样的潜在问题，一并走它。
- 新增文案全部进中英对照表（`--lang-audit` missing=0）。

## v1.0.12

- **托盘图标不再迟到 20 多秒**。启动路径上曾有三个**同步**调用挡在托盘图标创建之前：

  ```
  refresh_service_state() → seed_tunnels_wanted() → refresh_tunnels() → pystray.Icon(...)
  ```

  其中 `refresh_tunnels()` 要给每台 VM 起 ssh 并等探测结果——VM 不在线时**实测阻塞 21 秒**。后果是启动后的头 20 多秒里进程活着、却**一个托盘图标都没有**：纯托盘应用在这段时间完全无法操作，而用户看到的往往还是上一次被杀掉的实例留下的**幽灵图标**（点它没有任何反应，因为它的进程早就没了，而双击也永远不会产生 `ui: interpreter ready` 那行日志）。开机自启时最糟——登录时 VM 通常还没起来，于是每次开机的前 20 多秒托盘都是隐形的。

  修法：图标先立起来；`refresh_tunnels()` 移进 `monitor_loop` 的第一轮，并把那一轮改成**先探测、后等待**（原来是先等一个完整节拍）。另外两个调用留在同步路径上——它们是快的（本地健康探测、读配置），而且 `setup()` 里「ReMe 启动后启动隧道」的判断依赖前者的结果。

  **VM 不可达条件下的实测**（同一台机器、同一时刻）：

  | | 启动到托盘窗口出现 |
  |---|---|
  | 1.0.10 / 1.0.11（修复前） | **≥ 20.0 秒**（其中一个实例实测 29.3 秒） |
  | **1.0.12（修复后）** | **1.67 秒** |

  隧道探测照旧执行，只是不再挡路：同一实例在启动 22 秒后照常打印 `tunnel auto-reconnect … ok=False`，此时图标早已可用。

## v1.0.11

- **README 重写，并对齐已发布的产品**。发布链这一轮的三处改动把原来的 README 变成了**错的**：它教用户运行 `reme-helper-<版本>.exe`（exe 早已去掉版本号）、说配置住在 exe 旁边（早已搬到 `%LOCALAPPDATA%\reme-helper\`）、并且完全没提自更新。三处都是"照着做会撞墙"级别的错误。
  顺带按成熟项目的惯例重排：**核心功能在前**（配置、运行服务、VM 隧道、接入的客户端），安装与三种模式居中，**开发与发布压缩成一节**；自更新只在安装那一节带一句——它是附带能力，不是业务功能，跟 CI/CD 一样不必占版面。中英两份同步重写。

## v1.0.10

- **`build.bat` 里那六行「找不到文件」不是噪音，是真 bug**：套件把日志写在 `log\tests\`（`tests/conftest.py` 里的 `TOOL` 就是仓库根），而每一处 `type` 都在**仓库根**找。于是构建脚本自己那句「失败就看 `<日志>`」**从来没有生效过一次**，失败提示里写的也是同一个错路径。与打包检查是同一类缺陷、同一个文件——诊断信息指向一个文件不在那里的位置。
- **`release.bat` 现在会校验分支与远端**：它本来就拒绝脏工作区，但不检查「在哪个分支上」和「这个分支与远端是不是同一个提交」。这两件事在第一次发版前是**我手工查的**——而手工查的事情，迟早会不再有人查。现在除非分支是 `RELEASE_BRANCH`（默认 `main`，可用 `REME_RELEASE_BRANCH` 覆盖）且 `HEAD` 与上游是同一个提交，否则拒绝打 tag。比较的是 **SHA**，不是解析 `git status`——SHA 才是事实。

## v1.0.9

- **自更新根本不会完成（1.0.7 与 1.0.8 都中招）**：更新器脚本的等待循环写成 `tasklist /fi "…" | find /i "…"`，而它是被 `CREATE_NO_WINDOW | DETACHED_PROCESS` 启动的——**没有控制台**。在这个上下文里 cmd 的内部管道不工作：`tasklist.exe` 早已退出，而 `find.exe` 永久阻塞在 stdin 上不再返回。后果是更新**静默地永远不发生**：托盘已经说过「更新已开始，本窗口会关闭」，应用也真的退出了，然后就没有然后了——用户拿到一个关掉的程序和一个没做的更新。
  隔离实验：同一个脚本带控制台跑，管道 0.13 秒完成；换成产品实际的启动方式，无限挂起。把子进程 stdio 指向 `DEVNULL` **修不好**；去掉管道能修好。现在 `tasklist /nh` 写到文件、`find` 读那个文件，全程无管道。
  这个 bug 逃过了最初的自测，因为那次是在**控制台里手动跑那个 bat**——验了脚本逻辑，没验它被启动的上下文。
- **新增 `--check-helper-update` 与 `--helper-update`**：更新流程的无头孪生入口，与托盘那两个菜单项走**完全同一段代码**，只是不需要点菜单。加它的直接原因是这台机器上合成输入完全不落地（同 `--quit` 的理由），而更要紧的是：**要验的是发出去的那个 exe**，源码态验不到它。`--helper-update` 在交出更新后按更新器的要求立刻退出，好让它能替换文件。
- **诊断参数的输出固定成 UTF-8**：`--smoke` / `--release` / `--lang-audit` / 新增的两个更新参数，意义都是让脚本与 CI 读到结论，而结论是中文的。stdout 被重定向时 Python 用 locale 默认编码（本机 cp936）写出去，读的人只见乱码——测试套件在 CI 上踩的是同一个坑。冻结的 noconsole 态下没有 stream，这段整块包住。

## v1.0.8

- **装过 1.0.6 的机器升级后不再丢自启**：1.0.6 的 exe 带版本号，它的 `HKCU\...\Run` 值指向 `...\reme-helper-1.0.6\reme-helper-1.0.6.exe`。把新版**解压覆盖**到那个目录（手动替换，不是走更新器）之后，被指向的文件已经不存在，而没有任何东西会重写这个值——`set_autostart()` 只在用户点菜单切换时才跑。结果是下次开机静默失效，用户只会觉得「自启莫名其妙坏了」。现在启动时会把自启值修回当前 exe（`sync_autostart_path()`）：只在自启开着、且存的值与当前路径不一致时才写，因此幂等；顺带也修好「整个文件夹被移动/改名」。

## v1.0.7

- **第一次真发版暴露并修掉的三个问题，全在「本该解释失败」的那条链上**：
  - `--smoke` 断言了 `<ReMe 根>\config\app.yaml` 与 `app-full.yaml` 存在。这两个文件是应用**按需**写进用户自己那份 ReMe 的，官方 wheel 一份都不带（只有 `default` / `beam` / `demo` / `lme`），CI 上更没有 `config/` ⇒ 这两条在**任何干净机器上必假**，只在开发机上（应用早就写过）碰巧为真。它和紧挨着的注释（「别人装了官方 ReMe 时 config/ 里一份都没有」）本来就自相矛盾。真正的检查是那条上方的临时目录块：三种模式都要能**只靠官方包**推出来。
  - `main()` 结尾的 `os._exit()`（必须保留，否则 Tk 会以 `Tcl_AsyncDelete` abort）**不刷新 stdio 缓冲**。stdout 是控制台时无所谓；是**管道**时（CI 与所有构建脚本给的都是管道）Python 会块缓冲，`--smoke` / `--release` / `--make-icon` 的诊断随进程一起消失——CI 日志里连 `app_dir=...` 那行都没有。现在退出前手动 flush。
  - 打包检查失败时 `build.bat` 只写一句「See `<release>\log\smoke.log`」，而那个文件只存在于跑构建的那台机器上，CI 上随 runner 销毁。`--release` 那道门禁一直是正确的（goto + `type` 日志），smoke 与 ui-check 现在与它一致。
  - `--smoke` 的检查顺带改成**具名**，与 `--release` 一样报 `FAIL missing=<名字>`；以前是裸的 `True/False` 列表，定位失败项只能靠手数下标。

- **`--quit`：脚本可以请正在运行的实例退出了**：原来只有托盘菜单能停进程，而这台机器上合成输入完全不落地（`SendInput` 的移动与键盘都是空操作，只有 `SetCursorPos` 有效），agent 与远程操作者都点不动菜单——构建脚本因此卡死（`build.bat` 拒绝在任何 reme-helper 运行时构建：文件会被锁住，两个托盘还会抢同一份配置、服务与隧道）。`--quit` 只写一个请求文件就立刻返回；运行中的实例在专用循环里（1 秒节拍）发现它，走与托盘「退出」**完全相同**的清理路径（清理抽成了 `begin_shutdown()` / `shutdown_tray()`，只此一套）。
- **配置搬进用户数据目录**：`config.json` 从 exe 旁边移到 `%LOCALAPPDATA%\reme-helper\config.json`。理由是原地更新要能整目录替换——配置住在程序目录里，更新器就得为某一个文件写例外。首次运行时自动播种：新装用户拿到包里的出厂模板，老用户拿到自己那份（**只复制、不删除**，旧文件留作回退材料），幂等。
- **exe 定名 `reme-helper.exe`（不再带版本号）**：开机自启的注册表值存的是 exe 的完整路径，带版本号的名字会让每次原地更新都留下一个指向旧文件的残留项。发布目录与 zip 仍然带版本。构建脚本相应引入 `APPNAME`，并补了一条「包是不是空的」断言——`--name` 与 robocopy 的源目录一旦脱节，robocopy 会静默拷 0 个文件而返回码又低于 8。
- **打包自检读的是随包的出厂模板**：`build.bat` 用 `REME_HELPER_CONFIG` 把 `--smoke` / `--ui-check` / `--release` 指向发布目录里那份 `config.json`。没有这个覆盖，配置搬家后 `--release` 会读到开发机上的活配置，它自己的 `llm empty` / `no personal targets` 断言必然失败。
- **测试套件不再死在 runner 的编码上**：`tests/test_i18n.py` 在 CI 上报 `UnicodeEncodeError: 'charmap' codec can't encode characters`，本机却是过的。检查名是中文，而 runner 的 stdout 是管道 ⇒ Windows Python 用 locale 默认编码（cp1252）而不是 UTF-8 ⇒ 第一句非 ASCII 打印就把套件打死，根本轮不到报告。修在两层：`tests/conftest.py` 把 stdout/stderr `reconfigure` 成 UTF-8（`errors="replace"`，报告阶段永不崩），两个工作流加 job 级 `PYTHONUTF8=1`（套件之外的步骤也打印中文）。
- **手动触发 release 工作流现在一眼看得出来是「只构建」**：`run-name` 会写 `build only (no release)`。行为本来就是这样（发布那一步卡在 `refs/tags/v` 上），只是以前在列表里看不出来，容易被当成一次真发布。
- **开发态的 `src/config.json` 也进了 `.gitignore`**：应用在开发态把配置写在 `src/`，而忽略规则只盖了仓库根，于是保存过一次设置就会冒出一个带 LLM 端点与 VM 目标的未跟踪文件。

## v1.0.6

- **CI 真的跑得起来了（第一次推送是红的）**：两套工作流都先在工作机上装一份 ReMe（`H:\Tools\ReMe`，版本与开发机一致）再跑门禁。测试套件与打包自检都是**集成门禁**，不是纯单元测试：它们要读官方 `default.yaml`、要用 venv 里的 Python 报包版本号、要 `reme.exe` 存在——干净 runner 上这三样都没有，第一次推送的 `tests` 就挂在 `FileNotFoundError: 官方default配置不存在`。装 ReMe 的动作抽成 composite action `.github/actions/setup-reme`（`subst H:` 造出默认根目录，`pip install reme-ai` 不带 `core` extra——没有一道门禁会真的启动服务），两个工作流共用一份版本钉，并在装完后自检三个文件与版本号。
- **文档合并为单一 `doc/`**：删掉与之并存的 `docs/`，改为按语言分目录——`doc/zh/`、`doc/en/` 下各有 `setup.md`（接入文档）与 `configuration.md`（配置参考），`doc/capture.mjs` 是共用一份的 Codex 捕获脚本。两个近似同名的目录只会让人分不清「哪份要发、哪份给人看」；现在 `doc/` 既是运行时被应用读取的目录，也是唯一的文档目录。
- **Skill 只装一处**：`~/.agents/skills/<名字>/` 是用户级规范位置，Codex 与 DeepSeek Harness 都读它；此前 clear-code 同时存在于 `~/.agents/skills/` 与 `~/.codex/skills/`，实测在 Codex 里被加载**两次**（同名 skill 出现两遍），已删除 Codex 侧的副本。README 增加「Skill installation」一节写明这条规则。
- **README 重写为 GitHub 风格**：英文 `README.md` 为主、中文 `README.zh-CN.md` 同构，结构与 clear-code 对齐（定位一句话 + 配置什么 + 接入什么 + 开始使用 + 仓库结构 + 构建 + 数据边界），去掉评判性措辞，改为陈述本工具负责的部分。
- **隧道会自动重连（修掉两个真实故障）**：① 启动时 ReMe 已经在跑的话，`start_service` 会提前返回，挂在它后面的「启动隧道」根本不执行——「ReMe启动后启动隧道」这个勾选项等于失效，用户得自己去托盘点一次；② ssh 进程中途消失（网络抖动、VM 重启）时，旧实现只更新状态、从不重启，隧道断了就一直断着。现在 `TUNNEL_WANTED` 记住「这条隧道应该是连着的」：启动按配置播种，掉线后由健康检查接回去；用户手动停过的隧道不会被自动拉起。判断可用性改用直接的健康探测（`service_up`），而不是监控线程的缓存值。`test_helper.py` 新增 5 条不依赖真实 VM 的语义断言。
- **日志不再堆积、也不再散落在仓库根**：应用自己的日志移到用户数据目录（`%LOCALAPPDATA%\reme-helper\log\reme-helper.log`）并改为**滚动日志**（单文件 1MB、保留 3 份备份 `.1`…`.3`，约 4MB 上限）；单实例守卫之外的升级重叠场景会退回普通 FileHandler（Windows 下另一个进程持有文件时无法改名，宁可这次不滚也不要启动失败）。放到用户数据目录不是洁癖：应用运行时会一直持有该文件，放在包里则构建永远清不干净 release 目录（实测 rmdir 直接失败）。
- **运行期产物集中到 log/**：`--smoke` / `--ui-check` / `--release` / `--lang-audit` 的结论与 `i18n_review.md` 从程序目录根改到 `log/`，测试脚本的日志改到 `log/tests/`。开发态按仓库根走（`RUN_DIR`），不再把 `src/log`、`src/*.ico` 这类文件混进源码目录。发布包里不含任何 log 目录。
- **仓库结构整理**：源码进 `src/`（`main.py` / `i18n.py` / `guide.py`），测试进 `tests/`（新增 `conftest.py` 负责把 `src/` 加进 `sys.path`，并顶掉 `test_settings_ui.py` 里那条写死的本机绝对路径），工具脚本进 `scripts/`，维护者文档按语言分进 `docs/en`、`docs/zh`。根目录只留 `build.bat`（薄包装 → `scripts/build.bat`）与 `release.bat`（打标签触发 CI 发布）。`doc/` 保持原位：它是运行时被应用读取的接入文档，不能移动。
- **构建入口与 SCX 一致**：`build.bat` 直接构建并在**成功后自动启动新版本**（要只构建用 `build.bat norun`），`build.bat release` 额外跑打包版自检，`build.bat clean --force` 清缓存与产物。
- **中英双份 README**：`README.md`（英文为主，结构与 clear-code 对齐：一句话定位 + 为什么需要 + 能力 + 安装 + 构建 + 数据边界）与 `README.zh-CN.md`（同构中文），互相链接；配置参考也补了英文版 `docs/en/configuration.md`。
- **界面按使用顺序重排**：安装区四个按钮改成「复制安装提示词 · 打开官方文档 · **阅读接入文档** · **复制接入文档**」——先把 ReMe 装起来，再看怎么把客户端接上去；阅读在复制左边，符合「先看再交出去」的实际用法（提示文案里「左边的复制」也一并改掉了）。
- **文案去术语**：文档标题从「ReMe 接入作业书」改成直白的「把 Codex 与 DSH 接到 ReMe（接入 Agent 文档）」；「验收清单」改成「做完怎么确认」；英文侧的 `Integration Playbook` 改成 `setup guide`。界面里剩下的都是具体动作，不写行话。
- **发布目录结构**：产物统一到 `release\reme-helper-<版本>\`（不再按时间戳堆 `out\`），构建中间物收进 `.cache\`；同版本目录已存在时拒绝覆盖，保证一版一目录、可复现。`build.bat clean [--force]` 清缓存与产物。
- **发布配置脱敏**：发布包里的 `config.json` 由 `scripts/make_release_config.py` 从出厂默认值生成，不含开发机的 ReMe 路径、LLM/Embedding 端点与 Key、VM 目标；校验用**白名单**（每个值只能是空值/布尔/固定值），以后谁往默认值里加了个人数据都会让构建失败，而代码里不需要写下任何开发者标识。源码里的默认 VM 目标也换成了示例值（`ubuntu@192.168.1.100`）。
- **新增 `--release` 发布自检**：在冻结环境里校验出厂配置干净、图标与托盘菜单建得出来、此刻没有别的实例在跑、接入文档随包分发；`build.bat release` 会把它作为发布前的最后一道门。
- **仓库工程化**：`.gitignore`（构建产物、日志、本机 `config.json`）、MIT `LICENSE`、`requirements.txt`、GitHub Actions（`tests.yml` 每次推送跑全部测试；`release.yml` 打 `v*` 标签时构建并发布 zip + sha256）、`release.bat`（本地打标签并推送，构建交给 CI）。
- **文档重构**：README 改成面向使用者的入口文档，历次更新记录移到本文件，配置字段与变量等技术细节移到 `docs/zh/configuration.md`。

## v1.0.5

- **接入文档（`doc/`）+ 阅读/复制按钮**：把「把客户端接上 ReMe」这件事的完整做法写成一篇接入 Agent 文档，随工具分发（中英各一份，按界面语言取）。
  「运行与模式」区多了 **`阅读接入文档`** 与 **`复制接入文档`**（先读、再复制）：复制会把整篇（含附录 A 的 Codex 捕获脚本）放进剪贴板，提示粘贴给 AI 即可替你完成接入；
  阅读复用控制台说明的同一个窗口渲染，正文里的 `<REME_PORT>` / `<REME_WORKSPACE>` 会在打开/复制时换成当前配置的真实值。
  脚本在文件系统里只留一份（`doc/capture.mjs`），复制时才拼接，避免两处内容漂移；「路径与入口」窗口里也能找到这篇文档。
- **单实例保护**：托盘程序只允许一个实例。后启动的实例会先抢命名互斥体，抢不到就提示「已在运行」并退出——默认配置下工具会在启动时拉起 ReMe，
  两个实例同时起来会把「谁在管服务、谁在管隧道」搅乱。互斥体名字不带版本号，所以升级期间新旧版本也互斥。
  诊断参数（`--smoke` / `--ui-check` / `--release` / `--lang-audit` / `--make-icon`）不受影响，仍可在工具运行时执行。
- **发布打包（`build.bat`）**：版本号从 `main.py` 的 `VERSION` 读取（单一来源）；构建前拒绝任何版本的 `reme-helper-*.exe` 在跑；
  产物统一写到 `release/reme-helper-<版本>/`，同版本目录已存在就拒绝构建（一版一个目录，发布可复现）；`clean [--force]` 清缓存与产物。
  构建中间物全部收进 `.cache/`（PyInstaller 的 dist/work/spec 与按需创建的 venv），源码目录不再被 100MB+ 的 `build/` 污染。
  发布前逐项校验：`exe`、接入文档、捕获脚本、`smoke`、设置窗口 `--ui-check`、打包版 `--make-icon`，以及 `--release`（冻结环境 + 出厂配置干净 + 图标与托盘菜单建得出来）。
- **发布配置脱敏（`make_release_config.py`）**：发布目录里的 `config.json` 由出厂默认值生成，**不含**开发机的 ReMe 路径、LLM/Embedding 端点与 Key、VM 目标；
  脚本自带自检，一旦模板里还剩疑似本机数据就直接失败，不让它进发布包。
- **包体积优化**：排除本工具用不到的 `numpy` 与 PIL 的 `avif`/`webp` 解码器（实测合计约 34MB），整包从约 70.7MB 降到约 34.5MB。
- **首屏与措辞**：README 重写为面向使用者的入口文档，本文件承接历次更新记录；界面文案去掉内部术语，直接说做什么。

## v1.0.4

- **中英对照表重做（`i18n.py`）**：全部界面文案集中到一张表里，一行一条「中文 → English」，改文案时两列并排看就行。
  - 翻译按**最长片段**替换，所以「已保存：xxx（窗口保持打开）」这类拼接句、插在句子里的模式名都会整体变英文；未收录的文案自动回退中文，不会出现空白。
  - 托盘右键菜单改为**展开时求值**：切换语言后右键立刻是新语言（旧版菜单文案在启动那一刻就定死了，这就是「切了英文右键还是中文」的原因）。
  - `python main.py --lang-audit` 列出没翻的中文，并生成 `i18n_review.md`（左右对照表，方便逐条检查翻译是否得当）。
  - 两道自动化门禁：`test_i18n.py` 扫源码里出现的**每一条中文**，必须都能翻成不含中文的英文；`test_en_mode.py` 把控制台真的切到英文，逐屏扫描整棵控件树，任何中文残留都会红灯。
- **修掉「切换几次后控制台打不开」**：根因是每开一次窗口就新建一个 Tk 解释器，而 Tk 对象只属于创建它的线程——托盘回调跨线程碰 Tcl 会永久卡死托盘线程；隐式默认根窗口还会让新解释器抛 `main thread is not in main loop`，窗口建到一半就死了（打包版没有控制台，于是只表现为「点了没反应」）。现在全进程只有一个 Tk 解释器 + 一个常驻 UI 线程：任何线程要碰 Tk 都走请求队列，关窗只销毁窗口、切语言只重建窗口内容。`test_console_lifecycle.py` 专门回归这条路径（反复切语言/主题 8 次、关掉再开 3 次、重复点击复用同一个窗口）。
- **托盘菜单瘦身 + 打开指引**：托盘里 7 个「打开X」收敛成「打开 workspace」+「打开指引（文件在哪）…」；后者打开「路径与入口」窗口（目录 / workspace / 当前配置 / 官方 default / .env / 日志 / 说明，逐行可打开或复制路径）。测试锁死了这个形状：删掉的项目不允许再回来。
- **控制台说明改成内置内容**：以前是从 ReMe 安装目录读一份 Markdown——那份文件是某台机器上写的（路径、SSH 别名、验证日期都是私人的），普通用户装完 ReMe 也不会带着它，于是「说明」要么打不开、要么泄露你的目录结构。现在正文随工具分发（`guide.py`，讲通用做法，只有「本机路径」一节在打开时现场取值），并且整篇走同一张中英对照表，英文模式下自动是英文；测试会检查内置正文里不含机器专属路径。
- **API Key 按密码框的常规做法**：已经配置的 Key 会载入并显示为掩码，旁边一个「显示」开关可以看明文，留空＝不改、直接改这里即覆盖。以前字段是空的、只写「已配置」，看着像配置没读出来。
- **`.env` 与保存值不一致时以 .env 为准**：ReMe 真正读的是 .env，所以界面现在按 .env 校正（并提示校正了哪几项），保存后再写回 config.json，两边不会长期打架。
- **禁用态文字不再发虚**：浅色 `disabled` `#9ca3af`（对比度 2.2）与深色 `#6b7280`（3.4）都调亮到 4.5 左右，禁用按钮/链接的文字清晰可读但仍明显弱于正文。
- **主题一致性修复（浅色/深色）**：三个真实故障一起修掉——① 浅色原先用 Windows 原生 vista、深色用 clam，两个引擎的控件高度不同（27px vs 33px），切一次主题整页位移，而且 clam 下自建的分段按钮样式在 vista 里不存在、退化成黑块；现在两种主题共用同一个 ttk 引擎（`alt`），布局逐像素一致。② `restyle_widgets` 原来只改背景不改前景，深色切浅色后 `tk.Label` 还是近白字，浅底上「看不清」；现在按语义映射把前景色一起换（muted/warn/ok/link 仍是各自语义色），系统默认色一律用主题色兜底。③ clam 在这台机器上把勾选框「选中」画成 ✗、未选画成实心块，含义正好反了；换成 `alt` 后是标准的空框/绿色对勾。新增 `test_theme_ui.py` 当门禁：切主题前后逐控件比对几何（位移即红灯）+ 对比度检查（禁用态要求 ≥ 6:1、次要文字 ≥ 4:1，太低就是「发虚」的来源）。另外：托盘切主题时若没开着控制台，以前没人消费挂起的主题标志，接着打开的「打开指引」/说明/VM 对话框会用旧样式（看着还是上一个主题）——现在 UI 线程的请求泵每轮都会先落地主题，再建窗口。
- **故障不再静默**：Tk 回调里的异常会写日志并计数；`--ui-check` 改为「真的等窗口建出来 + 没有 UI 异常」才通过，静默失败会变成构建红灯。

## v1.0.3

- **深色模式**：控制台右上角按钮或托盘「深色模式」切换，选择会被记住；深色下使用 `clam` 主题（Windows 原生主题忽略颜色设置）。
- **中英切换**：控制台右上角 `English / 中文` 按钮或托盘「English / 中文」切换；覆盖托盘菜单、控制台主要界面、按钮、字段标签、功能表、弹窗与提示（词表见 `i18n.py`，一行一条中英对照；未收录的文案自动回退中文）。
- **「立即整理一次」防重复与结果回显**：运行期间按钮禁用（托盘/客户端触发不受影响），结束后从 ReMe 日志解析并显示上次整理结果（`写入 N 个节点（扫描 M 个文件、其中 K 个有变化）` 或 `判定无新内容，直接跳过`），可点「刷新整理记录」重读。
- **未安装 ReMe 的首屏引导**：状态条「服务」显示红色的**未安装**；保存按钮禁用并给出悬浮说明；托盘隐藏式提示「未检测到 ReMe —— 复制安装提示词」，并把启动/停止/重启等项按安装状态禁用。
- **按钮精简**：底部栏去掉`查看官方default`与`创建/打开.env`，LLM 区块去掉`写入 .env`（保存会自动写 .env），两个入口移到托盘的「打开…」组。

## v1.0.2

- **界面信息层级重做**：顶部状态条（模式 / 服务 / LLM / Embedding / 未保存改动）、编号区块标题 + 一句说明 + 细分割线、左标签列 + 右控件列、字号层级、宽屏内容列限宽居中；功能表按「记忆的产生 / 检索与成本 / 界面与接入」分组，效果摘要留在表里、完整说明进 `?` 悬浮。
- **LLM / Embedding / 记忆管道的真实测试**：LLM「测试连接」发带 JSON 要求的真实请求并判断结构化输出（auto_memory / auto_dream 依赖它）；Embedding「测试」检查端点确实提供 embedding、维度一致、以及近义对相似度高于无关对；测试前先自检凭据，缺哪个变量就点名。
- **记忆管道档位化**：扫描天数、单次最多沉淀、整理计划改成下拉档位，配实时效果预览（下次整理时间、扫描范围、调用上界、产出位置）。
- **保存行为**：`验证并保存` 成功后窗口保持打开、基线即时刷新、Toast 说明结果（含服务是否按新配置重启）；失败会回滚 `config.json`、`.env` 与运行中的服务模式。
- **回到基线与改动追踪**：基线＝你本次的起点；凭据永不参与复位；`待保存：…` 计数与禁用态门控。

## v1.0.1

- LLM / Embedding 配置注入生成的 ReMe 配置（`components.as_llm` / `components.as_embedding`），凭据写入 `.env`。
- 记忆管道参数（scan_days / max_units / dream_cron）可调并可即时整理。
- MCP 工具暴露（`service.jobs`）自定义允许列表：勾选后写入允许列表，不勾选沿用 ReMe 默认。
